# Contributing

感谢你帮助改进 NC2000 for BBK 9588。

## 开发流程

1. 初始化 SDK：`git submodule update --init sdk`。
2. 运行 `.\tools\build_bbk9588.ps1`。
3. 确认生成的 `build\nc2000_bbk9588\NC2000.bda` 通过打包器静态校验。
4. 涉及 JIT、NAND、音频或生命周期的改动，应同时在 SDK 模拟器和真机上验证。

请保持模拟器核心和桌面前端源码位于 `src/`，BBK 专用代码位于 `platform/bbk9588/`。
共享核心改动使用 `BBK9588` 条件编译，
避免破坏原项目的桌面构建。

## Issue 与日志

报告问题时请提供：

- 设备型号和 BBK 固件版本
- 使用的 NC2000/NC2600 内核名称或版本，不要上传固件文件
- 可重复的操作步骤
- `A:\NC2000\NC2000.LOG` 最后一次运行的相关片段
- 是否发生 BDA 退出、系统重启或必须断电恢复

## 禁止提交的数据

不要提交或附加 ROM、NAND、NOR、词典、官方软件、个人存档或其他专有数据。测试路径、
用户名、下载目录和设备序列号也应从日志与文档中移除。

## Pull request

- 一次 PR 聚焦一个问题。
- 说明测试过的构建目标和硬件环境。
- 不提交 `build/`、工具链、日志、音频抓取、ROM 或模拟器测试 NAND。
- 新增第三方代码时记录仓库、固定版本、许可证和本地修改。
