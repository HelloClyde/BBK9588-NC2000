# BBK 9588 assets

`nc2000_icon.png` is the source image for the BDA launcher icon. The packer
scales it to the device icon sizes during the build. It contains no emulator
firmware, NAND, NOR, dictionary, or other ROM data.

Locally generated chroma-key intermediates are intentionally excluded by the
repository `.gitignore`.
